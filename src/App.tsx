import QueryProvider from './providers/queryProvider'
import { Voting } from './Voting'

function App() {
  return (
    <QueryProvider>
      <Voting />
        </QueryProvider>
  )
}

export default App
